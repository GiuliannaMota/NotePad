# ===============================================
# GITIGNORE PARA SPRING BOOT/JAVA (MAVEN/GRADLE)
# ===============================================

# 1. ARTEFATOS DE BUILD E OUTPUTS
# -----------------------------------------------

# Maven (Build padrão e pastas geradas)
target/
HELP.md
.mvn/wrapper/maven-wrapper.jar
pom.xml.tag
pom.xml.versions
pom.xml.bak
release.properties
dependency-reduced-pom.xml
*.lock

# Gradle (Build padrão e pastas geradas)
/build/
/out/
.gradle/
!**/src/main/**/build/  # Exceção comum para alguns projetos Gradle
!**/src/test/**/build/

# Exceções de Output (Para garantir que não ignore targets aninhados, se existirem)
!**/src/main/**/target/
!**/src/test/**/target/


# 2. ARQUIVOS E CACHES DE IDES (IGNORAR TUDO)
# -----------------------------------------------

### IntelliJ IDEA ###
.idea/
*.iws
*.iml
*.ipr

### Eclipse / STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
# A pasta 'build/' do NetBeans já está coberta pela regra '/build/' na seção 1

### VS Code ###
.vscode/

# 3. SISTEMA OPERACIONAL
# -----------------------------------------------
.DS_Store
Thumbs.db

# -------------------------
# Regras do Angular (notepad-frontend)
# -------------------------

# Compiled output (Ajuste dos caminhos)
/notepad-frontend/dist
/notepad-frontend/tmp
/notepad-frontend/out-tsc
/notepad-frontend/bazel-out

# Node (Ajuste dos caminhos)
/notepad-frontend/node_modules
/notepad-frontend/npm-debug.log
/notepad-frontend/yarn-error.log

# IDEs and editors (Ajuste dos caminhos - Geralmente pode remover, mas mantendo por segurança)
/notepad-frontend/.idea/
/notepad-frontend/.project
/notepad-frontend/.classpath
/notepad-frontend/.c9/
/notepad-frontend/*.launch
/notepad-frontend/.settings/
/notepad-frontend/*.sublime-workspace

# Visual Studio Code (Ajuste dos caminhos)
/notepad-frontend/.vscode/*
!/notepad-frontend/.vscode/settings.json
!/notepad-frontend/.vscode/tasks.json
!/notepad-frontend/.vscode/launch.json
!/notepad-frontend/.vscode/extensions.json
/notepad-frontend/.history/*

# Miscellaneous (Ajuste dos caminhos)
/notepad-frontend/.angular/cache
/notepad-frontend/.sass-cache/
/notepad-frontend/connect.lock
/notepad-frontend/coverage
/notepad-frontend/libpeerconnection.log
/notepad-frontend/testem.log
/notepad-frontend/typings

# System files (Estes geralmente não precisam de prefixo, se estiverem na raiz ou no subdiretório)
.DS_Store
Thumbs.db